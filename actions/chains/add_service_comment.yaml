---
chain:
  - name: "add comment to Service"
    ref: "icinga2.add_comment"
    parameters:
      object_type: "Service"
      filter_string: "host.name==\"{{host}}\" && service.name==\"{{service}}\""
      author: "{{author}}"
      comment: "{{comment}}"
